output_paths:
  save_model_path: "{base_path}/resources/models/model.pkl"  # Ruta para guardar el modelo entrenado
  save_encoder_path: "{base_path}/Resources/encoders/encoder.pkl"  # Ruta para guardar los codificadores

input_paths:
  raw_data_path: "{base_path}/data/raw/DatosBajoCosto.csv"  # Ruta de los datos crudos


